resources:
  jobs:
    dbt_databricks_job_ShopFlow:
      name: dbt_databricks_job_ShopFlow
      tasks:
        - task_key: shop_Flow
          dbt_task:
            project_directory: /Workspace/Users/deepak.snfk.adm1@outlook.com/job-ready-dbt-databricks-data-engineering-project-1
            commands:
              - dbt deps
              - dbt seed
              - dbt snapshot
              - dbt run
              - dbt test
            schema: bronze
            warehouse_id: 9848ad35bbe8eacb
            catalog: workspace
            source: WORKSPACE
          environment_key: dbt-default
      queue:
        enabled: true
      environments:
        - environment_key: dbt-default
          spec:
            dependencies:
              - dbt-databricks>=1.0.0,<2.0.0
            environment_version: "5"
      performance_target: PERFORMANCE_OPTIMIZED

